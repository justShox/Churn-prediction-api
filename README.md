# 🏦 Bank Customer Churn Prediction

Сервис предсказания вероятности оттока клиентов банка на основе ML-модели CatBoost.  
Реализован в виде REST API с документацией Swagger и упакован в Docker-контейнер.

---

## 📊 Результаты модели

### Основные метрики (CatBoost tuned, calibrated)

| Метрика    | Train  | Test   | Gap     |
|------------|:------:|:------:|:-------:|
| ROC-AUC    | 0.9421 | 0.9347 | +0.0074 |
| AUC-PR     | 0.8234 | 0.8012 | +0.0222 |
| KS         | 0.7532 | 0.7389 | +0.0143 |
| F1         | 0.7321 | 0.7169 | +0.0152 |
| Precision  | 0.6342 | 0.6175 | +0.0167 |
| Recall     | 0.8601 | 0.8546 | +0.0055 |

### Сравнение моделей

| Метрика    | Logistic Regression | Decision Tree | Random Forest | CatBoost (tuned) |
|------------|:-------------------:|:-------------:|:-------------:|:----------------:|
| ROC-AUC    | 0.8823              | 0.9186        | 0.9298        | **0.9347**       |
| AUC-PR     | 0.6987              | 0.7788        | 0.7912        | **0.8012**       |
| KS         | 0.6361              | 0.6999        | 0.7215        | **0.7389**       |
| F1         | 0.6410              | 0.6924        | 0.7089        | **0.7169**       |
| Precision  | 0.5330              | 0.5914        | 0.6098        | **0.6175**       |
| Recall     | 0.8039              | 0.8350        | 0.8497        | **0.8546**       |

### Калибровка

| Метрика       | До калибровки | После калибровки |
|---------------|:-------------:|:----------------:|
| Brier Score   | 0.1082        | 0.1045           |

Калибровка через isotonic regression делает предсказанные вероятности более точными.

---

## 🗂️ Структура проекта
```
churn-prediction/
├── data/
│   ├── raw/                  # Исходные данные (TZ.csv)
│   ├── processed/            # Очищенный датасет после EDA
│   ├── train_test/           # Train/test сплиты (scaled + raw)
│   └── test/                 # Тестовые данные для API (10 записей)
├── models/
│   ├── catboost_model.pkl    # Калиброванная модель
│   ├── catboost_model_uncalibrated.pkl  # Оригинальная модель
│   └── scaler.pkl            # StandardScaler
├── notebooks/
│   ├── 01_EDA.ipynb          # Разведочный анализ данных
│   ├── 02_preprocessing.ipynb # Предобработка
│   ├── 03_modeling.ipynb     # Обучение, калибровка, сравнение моделей
│   ├── 04_validation.ipynb   # Валидация, SHAP, анализ порогов
│   ├── BUSINESS_IMPACT_ANALYSIS.md  # Анализ бизнес-эффекта
│   └── graphs/               # Визуализации
├── src/
│   ├── app.py                # FastAPI сервис
│   └── train.py              # Production скрипт обучения
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 🚀 Запуск проекта

### Вариант 1 — Docker (рекомендуется)
```bash
git clone <your-repo-url>
cd churn-prediction
docker compose up --build
```

### Вариант 2 — Локально
```bash
git clone <your-repo-url>
cd churn-prediction
pip install -r requirements.txt
python -m uvicorn src.app:app --reload
```

После запуска сервис доступен по адресу: `http://localhost:8000`  
Swagger документация: `http://localhost:8000/docs`

---

## 📡 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET   | `/`      | Проверка работы сервиса |
| GET   | `/health` | Health check |
| POST  | `/predict` | Предсказание для одного клиента |
| POST  | `/predict_batch` | Предсказание для нескольких клиентов |

### Пример запроса `/predict`
```bash
curl -X POST "http://localhost:8000/predict" \
     -H "Content-Type: application/json" \
     -d '{
       "кредитный_рейтинг": 620,
       "город": "Атырау",
       "пол": "Female",
       "возраст": 48,
       "стаж_в_банке": 3,
       "баланс_депозита": 95000,
       "число_продуктов": 1,
       "есть_кредитка": 1,
       "активный_клиент": 0,
       "оценочная_зарплата": 75000,
       "есть_депозит": 1
     }'
```

### Пример ответа
```json
{
  "churn_probability": 0.87,
  "prediction": 1,
  "risk_level": "high"
}
```

### Описание полей ответа

| Поле | Тип | Описание |
|------|-----|----------|
| `churn_probability` | float | Вероятность оттока от 0 до 1 (откалиброванная) |
| `prediction` | int | Бинарный прогноз (1 — уйдёт, 0 — останется) |
| `risk_level` | string | Уровень риска: `low` (<0.4), `medium` (0.4–0.7), `high` (>0.7) |

---

## 🔍 Ключевые выводы

### SHAP — топ факторов оттока
1. **Число продуктов** — 1 продукт = высокий риск, 2+ = удержание
2. **Возраст** — старше 45 лет = выше риск
3. **Активность** — неактивные клиенты уходят чаще
4. **Пол** — женщины уходят вдвое чаще (28.3% vs 14.1%)
5. **Город** — Атырау: 42% оттока vs 15-16% в других городах

### Портрет клиента группы риска
Женщина 45+, из Атырау, неактивная, с 1 продуктом и депозитом.

### Анализ порогов
| Threshold | Precision | Recall | F1    | FP   | FN  |
|-----------|-----------|--------|-------|------|-----|
| 0.3       | 0.45      | 0.92   | 0.60  | 1300 | 240 |
| 0.5       | 0.62      | 0.85   | 0.72  | 389  | 89  |
| 0.7       | 0.82      | 0.60   | 0.69  | 130  | 245 |

### Бизнес-эффект
- Без модели: 3,058 ушедших = полная потеря
- С моделью (threshold=0.5): удерживаем 2,615 клиентов (85.5%)
- ROI при threshold=0.5: **~248%**

---

## 🛠️ Стек технологий

- **ML:** CatBoost, Scikit-learn, SHAP, Optuna, scipy (KS-test)
- **API:** FastAPI, Uvicorn, Pydantic
- **Инфраструктура:** Docker, Docker Compose
- **Анализ данных:** Pandas, NumPy, Matplotlib, Seaborn
